var Utils = require('./lib/utils')
    assert = require('assert')
    Logger = require('../lib/logger.js');

var utils = new Utils();

describe('Test', function(){
  it("should do something", function(done) {
    /**
     * Write your test here
     *
     * utils.mockOperation('Endpoint', 'action', function(params, success, error) {
     *  success({status: 200, obj: { id: 100, other_attribute: 'Other' }});
     * });
     *
     * utils.runActivity({
     *   some_parameter: 100
     * }, function(error, response) {
     *   assert.ok(response.other_attribute);
     *   done();
     * });
     */
  });
});
